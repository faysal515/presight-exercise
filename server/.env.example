PORT=3000
LOG_LEVEL=info
# Production: logs to file (default ./logs/app.log). Override path:
# LOG_FILE=/var/log/presight/app.log

# Optional. If unset, an in-memory MongoDB is started via mongodb-memory-server.
# MONGODB_URI=mongodb://127.0.0.1:27017
# MONGODB_DB_NAME=presight
